# HTTP 代理
proxy = http://127.0.0.1:10808

# 如果你的代理需要认证（用户名/密码）
# proxy = http://user:password@127.0.0.1:7890

# HTTPS 代理（可选，curl会自动用上面的 proxy，但也可以单独指定）
# proxy = https://127.0.0.1:7890

# SOCKS5 代理
# proxy = socks5://127.0.0.1:1080
# proxy = socks5h://127.0.0.1:1080   # socks5h 表示 DNS 也通过代理解析
